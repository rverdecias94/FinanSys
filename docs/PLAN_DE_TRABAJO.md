# 🗺️ Plan de Trabajo — GESTIA / Sistema Contable

> Generado el **2026-06-17** a partir de una auditoría *council* multi-agente (44 agentes, ~2.2M tokens, 804 lecturas de código). Cada hallazgo **crítico/alto fue verificado por un segundo agente independiente** leyendo el código real (`archivo:línea`). Aquí solo se listan hallazgos **confirmados** o **parcialmente confirmados**; los refutados se descartaron.
>
> Salud por subsistema (0-100): Finanzas 62 · Almacén 62 · Inventario 66 · Reportes 68 · Auditorías 68 · RBAC 68 · Planes 68 · UI/Tema 62 · Supabase 58 · Calidad 74.
> Totales: **1 crítico, 29 altos, 35 medios, 39 bajos**.
>
> Salud técnica medida: `eslint` limpio ✅ · `build` OK (⚠️ bundle único 2.56 MB) · tests **25/25** ✅ (Sesión 7: +2 de `narrativeGenerator.test.js`/P2.5; `team.test.js` actualizado a upsert. Nota: en el run COMPLETO `PlansPanel.test.jsx` a veces falla por timing/race del harness — pasa en aislado).

Leyenda: 🔴 crítico · 🟠 alto · 🟡 medio · ⚪ bajo · ⏱️ esfuerzo (xs/s/m/l) · 🔑 = necesita acceso a Supabase (rellenar `.env.tooling`).
Estado de cada ítem: ⬜ pendiente · 🔄 en curso · ✅ hecho y probado · 🔶 propuesto (espera aprobación) · ⏭️ pospuesto.

---

## 🔁 Trabajo por sesiones (relay)
Trabajamos en cadena: cada sesión toma la siguiente prioridad del plan, la ejecuta, **prueba en localhost**, actualiza este documento y la memoria, y al final **genera el prompt de la siguiente sesión**. Así el contexto se mantiene fresco y el MCP de Supabase (que se carga al iniciar la sesión) queda disponible.

**Protocolo de cada sesión:**
1. Leer `CLAUDE.md`, este plan y la memoria del proyecto. No inventar; verificar en código/BD reales (cita `archivo:línea`).
2. Ejecutar la fase asignada. Cambios en BD de producción: proponer + probar en rama (branching) + aprobación explícita.
3. Probar en `localhost` antes de marcar nada como ✅.
4. Actualizar el **Registro de sesiones** (abajo) y los estados de los ítems.
5. Emitir el **prompt de la siguiente sesión** en un bloque copiable.

### 📓 Registro de sesiones
| Sesión | Fecha | Fase | Resultado |
|---|---|---|---|
| 10 | 2026-06-18 | P4 (higiene/código muerto/deps) + P5 (tests) + propuestas P3.8 y P4.1/P4.2 | ✅ MCP disponible (token válido, sin re-auth). **Commits `66d5e80` (P4) + `fd3980e` (P5) pusheados a `main`.** **P4.6** código muerto eliminado (verificado por grep, sin consumidores): `AppMejorado.jsx`, `pages/ReportesMejorado.jsx`, `services/reports.js`, `App.css`, `pages/index.js` (barrel). **El "2º PermissionGuard" NO se borró: ambos en uso** (`SubscriptionGuard.jsx` default → SidebarLayout; `common/PermissionGuard.jsx` named → páginas). **P4.7** deps sin uso fuera (`firebase`/`geist`/`moment`/`react-datepicker`/`@headlessui/react`/`react-scripts`/self-dep `sistema-citas`/`firebase-debug.log`); `name`→`gestia`; **hallazgo**: `react-scripts` era el único proveedor de `eslint`+`eslint-config-react-app` → añadidos como devDeps explícitas para no romper el lint; `npm install` −858 paquetes. **P4.4** opciones de auth explícitas en el cliente Supabase (sesión persiste, probado). **P4.5** guard `updatePlan('free')`→asistente (client-only; verificado en localhost que abre el wizard, no downgrade directo). **P4.3** `schema.sql` raíz obsoleto→cuarentena (`_PELIGROSO_NO_USAR`). **P5.1** flake resuelto con `globals:true`+cleanup global (3 corridas 27/27 estables). **P5.2** +9 tests (TransactionModal readonly/moneda, notifications 23514/53400/…) → **suite 36/36**. **P4.1/P4.2 VERIFICADO read-only**: `pg_policies` de prod **sanos** (sin `anon`, 0 escrituras permisivas, advisor 0 ERROR); pero la carpeta local `migrations/` (~63) NO está sincronizada con el remoto (34 aplicadas) → **propuesta de higiene documental, NO aplicada** (despliegues van por MCP, no por CLI → sin riesgo activo). **P3.8 APLICADO** (Roberto aprobó "rediseño completo"): `Dashboard.jsx` reescrito — héroe Balance + 3 tarjetas (Entró/Salió/Resultado) con +/− y color, selector de moneda único del panel, lenguaje llano "¿Cómo va tu negocio?", empty state guiado en 3 pasos, Pie→donut, icono dinámico por signo. **Verificado en localhost móvil 375px + desktop** (tarjetas de igual altura, sin overflow, ambos estados vía `?empty=1` temporal revertido; consola sin errores). **P4.1/P4.2 DIFERIDO** (Roberto eligió diferir; RLS de prod ya verificado sano). Lint limpio, build OK, **suite 36/36**, smoke completo en localhost. Sin cambios de BD. |
| 7 | 2026-06-18 | P2 (seguridad export/auditoría) + cierre P1.15 | ✅ MCP disponible (token previo válido, sin re-auth). **P1.15** RESUELTO (función `check_email_availability`: "dueño"=contenido real, ya no cuenta áreas/monedas vacías; `inviteMember`→upsert para reactivar `revoked`); probado en vivo reversible sobre roberyani94 (cascarón vacío→reinvitable, +producto→bloqueado; BD restaurada). **P2.2** PII fuera de `audit_logs.details` (solo metadatos) + **9 logs históricos depurados** (aprobado); vista de Logs probada a 375px sin fuga ni overflow. **P2.3** índice `idx_audit_logs_business_created` (EXPLAIN→Index Scan). **P2.5** try/catch en `fmt`/`formatCurrency` (+unit test). **P2.6** cerrado listado anónimo de `company-logos` (anon list→0, URL pública→200). 3 migraciones nuevas (`p1_15`, `p2_3`, `p2_6`). **Suite 25/25 verde** (los 3 fallos esporádicos de `PlansPanel` son flake de timing del run completo; pasa en aislado). Lint limpio. Advisor **0 ERROR**. BD prístina. **P2.4 pospuesto a Sesión 8** (cambio de dependencia `xlsx`). **+ Mejora de Logs pedida por Roberto** (commits `34e522b`, `0fc9be1`): botón **"Ver detalles"** en Auditoría (Dialog en tabla desktop + tarjetas móvil, etiquetas en español, sin necesidad de Excel; probado 375px+desktop sin overflow); **logs sin UUID** — `team.js` registra correo del miembro/nombre de rol (no `Miembro ID:`/`role_id`), y se omite `user_id` en los logs full-row de Almacén/Inventario (extensión de P2.2). **Historial depurado**: 0 UUIDs en `resource` y `details` de toda la tabla `audit_logs` (verificado). Pendiente going-forward menor: los logs de **Planes** ("Solicitar/Aprobar Plan") con `request_id` son **server-side** (RPC) → aún registran ese UUID; flag para Sesión 8. |
| 9 | 2026-06-19 | P3.5–P3.8 (rendimiento + pulido premium) | ✅ client-only, sin BD. **P3.5** (`bff1b90`): **code-splitting** — `React.lazy`+`Suspense` por ruta (Suspense en el `<Outlet/>` para que el sidebar persista), `manualChunks` (charts/export-vendor/supabase/router), `import()` dinámico de `exportUtils` en finanzas/logs → **bundle inicial 2,612 KB → ~150 KB index + 170 KB supabase (~90 KB gzip)**; `export-vendor` (455 KB gzip) y `charts` (160 KB gzip) bajo demanda. **P3.7** (`d592d96`): hook `useChartAnimation` (400ms + `prefers-reduced-motion` + guard >60 pts) en las 5 series de Dashboard/AlmacenDashboard. **P3.6** (`ea135c3`): contraste WCAG — verde `--success` claro 2.54:1 (FALLA AA) → `160 90% 26%` = 4.98:1 (oscuro ya OK); signos +/− en importes de Finanzas (lista+resumen). Suite **27/27**, lint limpio, navegación verificada en localhost. **P3.8 DIFERIDO** (rediseño dashboard "¿cómo voy?": ⚪ grande, requiere propuesta de diseño con Roberto antes de aplicar). |
| 8 | 2026-06-19 | P2.4 + premium/robustez (P3.1–P3.4) | ✅ MCP disponible (token válido; P2.4/P3.1–P3.4 son client-only). **P2.4** (`e2b0b87`): `xlsx@0.18.5`→tarball oficial **SheetJS 0.20.3** (CVEs solo en el parser `read`, que no usamos; fix vendor-recomendado, API idéntica). Verificado: `npm ls`=0.20.3, test escape 3/3, build OK, runtime localhost generó `.xlsx` 16KB. **P3.4** (`093ba12`): **ErrorBoundary global** premium (tokens, mobile-first, Recargar/Volver + Detalles técnicos colapsable); test 2/2 + probado forzando `?boom=1` (desktop+375px). **P3.1** (`6419ef5`): fuente **Outfit** unificada y realmente cargada (`fonts.check`=true; Tailwind sans/heading + h1-h6 + index.html). **P3.2** (`a9e6484`): colores ingresos/gastos a tokens `success/destructive` en Finanzas/Reportes/MovementList (iconos export Excel/PDF se dejan); verificado oscuro+claro. **P3.3** (`b85bcfe`): páginas de contraseña a tokens (sin caja blanca en oscuro; `.bg-white`=0); verificado oscuro en ambas páginas+formulario. **Suite 27/27** (incl. 2 tests nuevos de ErrorBoundary), lint limpio. Sin cambios de BD. **Pendiente Sesión 9**: P3.5–P3.8 (code-splitting, daltonismo, animaciones, dashboard), P4 (higiene/código muerto/deps), P5 (tests). Flag menor: logs server-side de Planes (`request_id`) y P0.7 (leaked-password, requiere Supabase Pro). |
| 6 | 2026-06-18 | P1 cierre (P1.11, P1.12, P1.13) | ✅ MCP disponible (token previo válido, sin re-auth). **P1.11** (límites de plan server-side, `20260618_p1_11_plan_limits_currencies_partners_serverside.sql`): triggers `BEFORE INSERT/UPDATE` en `business_currencies` (monedas) y `BEFORE INSERT` en `team_members` (socios) + `get_effective_plan_id` (honra expiración) + `currencies` en `plans.limits`; SQLSTATE `53400`→mensaje amigable. Probado REST cuenta free real (2ª moneda bloqueada) + `DO`/`ROLLBACK` (premium cortado en 6º). **P1.12** (`finanzas.js`: `else if expense`) verificado por unit test (3/3, tipo `transfer` ignorado) + render localhost. **P1.13** (`TransactionModal.jsx`: zod regex + finite + guard `NaN`) probado a 375px/desktop (Infinity/hex/1e3/-5 rechazados) + **fix pixel-perfect** de alturas (selects/fecha→`h-11`). Suite **23/23 verde**; advisor **0 ERROR**; BD prístina. Commit `06d3695` en `main`. |
| 6b | 2026-06-18 | UX Configuración (P3.10) + decisión P1.14 | ✅ (continuación de la Sesión 6, sin BD). **P1.14 RESUELTO**: con investigación web (QuickBooks/Xero/Thomson Reuters; SOX/auditoría) se decide **no permitir borrado físico** de transacciones (inmutabilidad contable); correcciones vía ajuste, eventual "Anular" soft-delete. **P3.10 (lote Configuración)**: C1 botones de balance al final (`BalanceBaseSection`), C2 "Activar"+tooltip en Monedas (`CurrenciesPanel`), C3 estados Activo/Pendiente/Revocado (`TeamManagement`), C5 permisos en español en tarjetas de Roles (`RoleManagement`), C6 quitar tab Permisos (`ConfiguracionMejorado`), C7 "Ver permisos" en roles del sistema vía `RoleModal` `readOnly`. **Probado a 375px y desktop** (sin overflow, alineado; modal readonly con 22 permisos deshabilitados y footer "Cerrar"; tabs sin Permisos; Equipo muestra "Revocado"). Lint limpio. Pendiente al cierre: commit/push a `main`. |
| 1 | 2026-06-17 | Análisis + auditoría + conexiones + memoria | ✅ Plan, `CONEXIONES.md`, `CLAUDE.md`, MCP configurado, RLS anónima verificada (0 filas) |
| 2 | 2026-06-17 | P0 — Seguridad RLS | ✅ Verificación en BD vía MCP (RLS ENABLED en todas las tablas; sin políticas `anon`; aislamiento por `business_id` OK) **+ hardening APLICADO con aprobación de Roberto** (`20260617_p0_security_hardening.sql`): P0.4 `is_premium` search_path, P0.5 +pg_temp, P0.2 REVOKE TRUNCATE/TRIGGER/REFERENCES a authenticated **y anon**, `plans` RLS. Post-verificado (proconfig, grants, advisor sin ERROR). 3 scripts peligrosos en cuarentena. **P0.1/P0.2/P0.3/P0.4/P0.5/plans = ✅**. Pendiente: smoke test en localhost + P0.6 (bucket transaction-images), P0.7 (leaked password), P0.8 (9 funciones SECURITY DEFINER sin search_path). |
| 5 | 2026-06-18 | P1 (P1.9, P1.10) + UX inventario (P3.9) | ✅ **+ UX inventario (P3.9)**: bug del asterisco de requeridos en `FormRunner` (solo texto lo mostraba → ahora todos los tipos) y tipos menos técnicos + pista por tipo en `FormBuilder` ("Booleano"→"Sí/No", "Área de Texto"→"Texto largo", "Selección"→"Lista de opciones"); probado en localhost en un área vacía. **Aplicados+verificados en vivo con aprobación de Roberto** (3 migraciones remotas: `p1_9_rename_inventory_field_rekey_values`, `p1_10_enable_pg_cron`, `p1_10_premium_auto_expiry_function` + `cron.schedule`): **P1.9** (RPC `rename_inventory_field` re-indexa los `values` de los ítems al renombrar un campo; enfoque B, sin migrar datos retro porque no había huérfanos; `FormBuilder` la llama en el onBlur del nombre), **P1.10** (cliente: `normalizeExpiredPremium` en `SubscriptionContext` + guard en `isPremium()`; BD: `pg_cron` + `expire_premium_subscriptions_auto` sin gate de admin agendada a diario). **Probado en localhost** (rename conserva el valor del ítem; premium vencido oculta pestañas premium) + REST (re-index/colisión/permiso; expiración real sin tocar el premium vigente). Hallazgo: ningún consumidor usaba `isPremium()` → la cron + normalización son el fix app-wide. BD prístina (sujetos de prueba revertidos al estado exacto; owner sigue premium 2027). Advisor **0 ERROR**. Lint limpio. |
| 4 | 2026-06-18 | P1 datos/inventario (P1.4, P1.6, P1.7, P1.8) | ✅ MCP disponible (token previo válido, sin re-auth). **Aplicados+verificados en vivo con aprobación de Roberto** (3 migraciones nuevas, remotas `p1_4_inventory_items_align_rbac_crud`, `p1_6_p1_7_atomic_stock_movement`, `p1_8_harden_create_inventory_item_atomic`): **P1.4** (RLS `inventory_items` de `inventory.move`→granular create/edit/delete, alineada con la UI; reproducido 403 con miembro+rol custom sin move, tras fix INSERT 201/UPDATE 200/DELETE 204; ningún rol pierde acceso), **P1.6+P1.7** (RPC atómica `register_stock_movement` + CHECK `stock>=0`; sin race read-modify-write, salida insuficiente rechazada con mensaje claro), **P1.8** (`create_inventory_item` endurecida: permiso `inventory.create`+propiedad, SKU+INSERT atómico vía `generate_inventory_sku` en una transacción → sin huecos). Cliente: `almacen.js` y `dynamicInventory.js` usan las RPC; `notifications.jsx` muestra mensaje amigable para `23514` (antes en prod ocultaba el motivo). **Probado en localhost** con cuentas reales (owner + miembro custom). BD dejada prístina (artefactos de prueba revertidos; miembro vuelto a Editor/'revoked'). Advisor: **0 ERROR**. Lint limpio. |
| 3 | 2026-06-17 | Cierre P0 + inicio P1 | ✅ MCP disponible (token Sesión 2 válido, sin re-auth). **Smoke test en localhost SUPERADO** con cuenta real (login/INSERT/UPDATE/Configuración/Admin-Planes; el REVOKE de S2 no rompió nada; hallazgo P1.14: sin borrado en UI). **Aplicados+verificados en vivo con aprobación de Roberto:** **P0.8** (search_path en 10 funcs; advisor mutable 10→0), **P0.6** (cierre de listado anónimo del bucket transaction-images sin romper visualización; advisor 2→1), **P1.1** (bug de moneda, probado: registra en EUR), **P1.3** (fuga RBAC business_settings, fuga reproducida y cerrada con cuentas reales), **P1.2** (modo solo-lectura del modal vía `<fieldset disabled>`, probado con botón "Ver"), **P1.5** (admin delegado crea roles: `owner_id`→`businessId`; verificado REST 403/201 + UI dueño), **P2.1** (escape de inyección Excel en `exportToExcel`; test unitario 3/3 + smoke). 3 migraciones nuevas en `supabase/migrations/`. Advisor: **0 ERROR**, 131→120. **P0.7 POSPUESTO** (requiere plan Pro de Supabase, confirmado por Roberto). Commits `836fbf5` + `bc7f671` (+ P2.1) en `main`, pusheados a GitHub. |

---

## P0 — Seguridad de datos (RLS) · URGENTE 🔑
**Por qué primero:** son datos financieros de tus clientes. Si alguno de estos scripts se aplicó a producción, los datos podrían estar expuestos. Lo primero NO es cambiar código sino **verificar el estado real de la BD**.

> **✅ Verificado en BD de producción vía MCP de Supabase (Sesión 2, 2026-06-17).** Project ref `obsxmmwrzsaahjmlhtxd`. Consultas: `pg_class.relrowsecurity`, `pg_policies`, `information_schema.role_table_grants`, `pg_proc.proconfig`, `get_advisors(security)`. Resultados por ítem abajo.

| # | Estado | Hallazgo | Evidencia / Verificación | ⏱️ |
|---|---|---|---|---|
| P0.1 | ✅ | `disable_rls.sql` desactivaba RLS en tablas clave. **VERIFICADO: RLS está ENABLED** (`relrowsecurity=true`) en **todas** las tablas de negocio (`transactions, products, movements, inventory_*, business_*, team_members, audit_logs`, etc.). Aislamiento por `business_id` correcto: las políticas usan `has_permission_secure(user_id, …)` y `get_current_business_id()`, que para `anon` devuelven falso (0 filas, confirmado Sesión 1). Único caso RLS off: `plans` (catálogo sin PII). | BD: `pg_class` (24 tablas, solo `plans` off). `rbac_hardening_v2.sql:371-545`. Script peligroso en cuarentena. | s |
| P0.2 | ✅ | `grant_all_public.sql` daba `GRANT ALL`. **CONFIRMADO ACTIVO** y además `anon` también lo tenía (más grave). **APLICADO Y VERIFICADO** (`20260617_p0_security_hardening.sql`): `REVOKE TRUNCATE, TRIGGER, REFERENCES … FROM authenticated, anon`. Tras el cambio ambos quedan solo con `DELETE, INSERT, SELECT, UPDATE` (gateados por RLS); `service_role` intacto. Follow-up abierto: evaluar revocar también `INSERT/UPDATE/DELETE` de `anon` (no usado pre-login). | BD: `role_table_grants` antes/después. Script en cuarentena. | m |
| P0.3 | ✅ | `allow_anon_transactions.sql` daba a `anon` insert/lectura. **VERIFICADO RESUELTO**: NO existen `transactions_insert_anon`/`transactions_read_anon` en `pg_policies`. Las 4 políticas de `transactions` están gateadas por `has_permission_secure(...)`. | BD: `pg_policies` (transactions: read/insert/update/delete, todas con permiso). Script en cuarentena. | s |
| P0.4 | ✅ | `is_premium()` `SECURITY DEFINER` **sin** `SET search_path`, usada dentro de RLS (`team_members_manage`). **APLICADO Y VERIFICADO** (`20260617_p0_security_hardening.sql`): `ALTER FUNCTION public.is_premium(uuid) SET search_path=public,pg_temp`. `pg_proc.proconfig` ahora = `{search_path=public, pg_temp}`; ya no aparece en `function_search_path_mutable`. | BD: `pg_proc.proconfig` antes/después. `20260213_plans_and_teams.sql:109-127`. | xs |
| P0.5 | ✅ | `check_email_availability` enumeraba cuentas. **VERIFICADO MITIGADO + reforzado**: la versión desplegada ya devolvía **solo booleano**; se añadió `pg_temp` al search_path (`20260617_p0_security_hardening.sql`). Residual: enumeración inherente a "¿email disponible?" (bajo). | BD: `pg_proc.proconfig`. `20260302_fix_check_email_availability_security.sql:1-7`. | s |
| **Acción** | ✅ | **Cuarentenar** scripts peligrosos del repo y documentar canónico. **HECHO**: `disable_rls.sql`, `20240202_allow_anon_transactions.sql`, `20260301_grant_all_public.sql` movidos a `supabase/_PELIGROSO_NO_USAR/` con README; `rbac_hardening_v2.sql` declarado estado canónico de RLS. | `supabase/_PELIGROSO_NO_USAR/README.md` | s |
| P0.6 | ✅ | **APLICADO Y VERIFICADO EN VIVO (Sesión 3)** (`20260617_p0_6_close_bucket_listing.sql`, remoto `p0_6_close_transaction_images_anon_listing`). Confirmado mecanismo: la app sube con `getPublicUrl` (`finanzas.js:24-28`) y muestra con `<img src>`; servir por URL pública **no pasa por RLS** (doc Supabase), sólo `list` la usa. Reproducida la fuga: con anon key se listaba la carpeta (user_id) y el comprobante. Fix: política SELECT restringida a `authenticated` sobre su propia carpeta (patrón company-logos); INSERT/DELETE intactos. Post-prueba: URL pública 200 (visualización intacta), listado anónimo → 0, advisor `public_bucket_allows_listing` 2→1. **Pendiente menor**: `company-logos` (menos sensible) → ver P2.6. | `20260617_p0_6_close_bucket_listing.sql`; `get_advisors`. | s 🔑 |
| P0.7 | ⏭️ | **POSPUESTO (Sesión 3): requiere plan Pro de Supabase.** `auth_leaked_password_protection` (HaveIBeenPwned) sólo es configurable en Pro+; el proyecto está en Free → el toggle aparece bloqueado. Reactivar este ítem cuando se suba a Pro: Authentication → Providers → Email → "Prevent use of leaked passwords". No bloqueante. | `get_advisors(security)` WARN (queda en 1); doc: requiere Pro Plan. | xs |
| P0.8 | ✅ | **APLICADO Y VERIFICADO (Sesión 3)** (`20260617_p0_8_function_search_path.sql`, remoto `p0_8_pin_search_path_remaining_funcs`): `ALTER FUNCTION … SET search_path=public,pg_temp` en lote para las 9 SECURITY DEFINER + el trigger `touch_business_settings_updated_at`. Verificado: `pg_proc.proconfig` = `{search_path=public, pg_temp}` en las 10; advisor `function_search_path_mutable` 10→**0**; 0 ERROR. | `20260617_p0_8_function_search_path.sql`; `pg_proc.proconfig`. | s 🔑 |
| plans | ✅ | Único `rls_disabled_in_public` (ERROR del advisor). **APLICADO** (`20260617_p0_security_hardening.sql`): `ENABLE ROW LEVEL SECURITY` + política `plans_read_all` (SELECT `USING(true)`). Lectura idéntica a antes; escrituras del cliente bloqueadas (solo `service_role`). El advisor ya **no reporta ERROR**. | `get_advisors`; `pg_class.relrowsecurity`. | xs |

> **✅ APLICADO Y VERIFICADO en producción (Sesión 2, 2026-06-17)** — con aprobación explícita de Roberto. Migración `20260617_p0_security_hardening.sql` (registrada en remoto como `p0_security_hardening`, v20260617215746). Cambios idempotentes y con rollback documentado en el propio archivo.
>
> **✅ P0 CERRADO casi por completo (Sesión 3, 2026-06-17)** — con aprobación explícita de Roberto. **Smoke test en localhost SUPERADO** (cuenta owner real): login ✅, crear transacción (INSERT) ✅, editar (UPDATE) ✅, abrir Configuración ✅, abrir Admin→Planes ✅ → el REVOKE de la Sesión 2 **no rompió nada**. (Hallazgo: la UI de Finanzas **no tiene borrado** de transacciones — sólo Editar/Ver; no es fallo del REVOKE, ver P-nuevo abajo). Aplicados y verificados en vivo: **P0.8** y **P0.6** (migraciones `20260617_p0_8_function_search_path.sql`, `20260617_p0_6_close_bucket_listing.sql`). **Único pendiente de P0: P0.7** (toggle de leaked-password en el dashboard, lo hace Roberto). Advisor: **0 ERROR**, total 131→120.

---

## P1 — RBAC y corrección de datos (lo que rompe tu promesa de roles y la exactitud contable)

| # | Hallazgo | Evidencia | ⏱️ |
|---|---|---|---|
| P1.1 | ✅ **HECHO Y PROBADO EN LOCALHOST (Sesión 3)**. **Bug de moneda**: la prop se pasaba como `businessCurrencies={...}` pero `TransactionModal` la recibe como `currencies` → caía a `[]` y luego a **USD** fijo. Fix de 1 línea en `FinanzasMejorado.jsx:477` (`currencies={businessCurrencies}`). Probado con cuenta real: activé EUR, el modal ofreció USD+EUR, creé/edité una transacción y se guardó con `currency=EUR` (verificado en BD). Lint limpio. | `TransactionModal.jsx:75` vs `FinanzasMejorado.jsx:477` | **xs** |
| P1.2 | ✅ **HECHO Y PROBADO EN LOCALHOST (Sesión 3)**. **Modo solo-lectura**: `FinanzasMejorado.jsx:478` ya pasaba `readonly` pero `TransactionModal` lo ignoraba → desde "Ver" se podía editar y guardar. Fix: el modal recibe `readonly`, envuelve los campos en `<fieldset disabled>` (deshabilita todo nativamente), `handleSubmit` hace early-return y el footer muestra solo "Cerrar". Probado con cuenta real: botón "Ver" → título "Ver Movimiento", campos `:disabled`, selector de moneda no abre, sin botón de guardar. Lint limpio. | `TransactionModal.jsx` (ahora usa `readonly`); `FinanzasMejorado.jsx:142,478` | s |
| P1.3 | ✅ **HECHO Y VERIFICADO EN VIVO (Sesión 3)** (`20260617_p1_3_business_settings_rbac.sql`, remoto `p1_3_business_settings_require_config_view`). **Fuga RBAC** reproducida: un miembro (Editor, sin `config.view`) leía `business_settings` del dueño por API REST (200, 1 fila) aunque el cliente lo bloqueaba. Fix: política `business_settings_read` con `has_permission_secure(user_id,'config.view')`. Post-prueba con cuentas reales: miembro → 0 filas (cerrado), dueño → 1 fila (intacto). Sólo se lee en `/configuracion` (ya exige `config.view`). | `20260607_business_settings.sql` (origen: `team_member_read_config_data`) → reemplazada por `20260617_p1_3_business_settings_rbac.sql` | s 🔑 |
| P1.14 | ✅ **RESUELTO (Sesión 6): intencional, NO se implementa borrado físico.** Decisión de Roberto con base en buenas prácticas de sistemas contables profesionales (QuickBooks, Xero, Thomson Reuters) y auditoría/SOX: los registros financieros deben ser **inmutables** y con rastro de auditoría a prueba de manipulación; el borrado físico lo rompe. Las correcciones se hacen con un **movimiento de ajuste/contrario**; si en el futuro se quiere "quitar" un movimiento, sería vía **Anular/soft-delete** (conserva el registro, lo excluye del balance), nunca DELETE. No se añade botón de borrado. Fuentes: Patriot (void vs delete), Thomson Reuters, BetterCloud/Imperva (SOX). | `FinanzasMejorado.jsx` (sin delete, por diseño) | — |
| P1.4 | ✅ **HECHO Y PROBADO (Sesión 4)** (`20260618_p1_4_inventory_items_align_rbac_crud.sql`). **Divergencia RBAC**: la UI usa `inventory.create/edit/delete` (`FormRunner.jsx:128,314,319`) pero la RLS de `inventory_items` exigía `inventory.move` (`FOR ALL`). `inventory.move` no se usa en cliente; las tablas hermanas ya usaban create/edit/delete. **Fuente de verdad = UI + catálogo**; se sustituyó la política `FOR ALL` por 3 granulares (insert→create, update→edit, delete→delete). Reproducido el bug con miembro real + rol custom sin move (**403**), tras fix **INSERT 201/UPDATE 200/DELETE 204**. Ningún rol pierde acceso (Administrador/Editor tienen create/edit/delete). `inventory.move` queda huérfano en el módulo (limpieza P4). | `FormRunner.jsx:128,314,319` vs `rbac_hardening_v2.sql:474-480` | m 🔑 |
| P1.5 | ✅ **HECHO Y PROBADO (Sesión 3)**. **Admin delegado no podía crear roles**: `RoleModal:88` mandaba `owner_id = session.user.id` (miembro), pero la RLS `roles_manage` exige `owner_id = get_current_business_id()` (dueño). Fix: usar `businessId` de `useBusiness()` (= dueño tanto para owner como para miembro). Probado: REST con un admin delegado (team.manage) → `owner_id=member` da **403 RLS**, `owner_id=business` da **201**; y el dueño sigue creando roles desde la UI con `owner_id` correcto (sin regresión). Lint limpio. | `RoleModal.jsx` (ahora usa `businessId`); RLS `roles_manage` | s |
| P1.6 | ✅ **HECHO Y PROBADO (Sesión 4)** (`20260618_p1_6_p1_7_atomic_stock_movement.sql`). **Race condition de stock**: el read-modify-write se sustituyó por la RPC `register_stock_movement` (un solo `UPDATE` bajo lock de fila + INSERT del movimiento en una transacción). `almacen.js registerMovement` ahora la llama. Verificado REST (entrada/salida correctas) + localhost (entrada 20→25 por UI). | `src/services/almacen.js:192-220` → RPC | m 🔑 |
| P1.7 | ✅ **HECHO Y PROBADO (Sesión 4)** (misma migración). **Stock negativo**: `CHECK (stock>=0)` en `products` (backstop, 0 filas en conflicto) + la RPC rechaza salidas que dejarían negativo con mensaje claro. Verificado: salida excesiva → "Stock insuficiente" (REST 400) y en UI toast "El stock no puede quedar en negativo" (mejora en `notifications.jsx` para el código `23514`, antes oculto en prod). | `almacen.js:210-218`; `20250207_create_almacen_schema.sql:27` | s 🔑 |
| P1.8 | ✅ **HECHO Y PROBADO (Sesión 4)** (`20260618_p1_8_harden_create_inventory_item_atomic.sql`). **SKU no atómico**: `createItem` usaba `generate_inventory_sku`+INSERT separado → huecos. Se **endureció** `create_inventory_item` (permiso `inventory.create` + propiedad, evitando bypass de RLS) y se usa SKU+INSERT en una transacción. `dynamicInventory.js` ahora la llama. Verificado REST (SKU contiguo ELEC-0005/0006; p_user ajeno → 403) + localhost (owner crea ítem, sin errores). | `dynamicInventory.js:224-244`; `20260209_inventory_sku.sql:131-134` | s 🔑 |
| P1.9 | ✅ **HECHO Y PROBADO (Sesión 5)** (`20260618_p1_9_rename_inventory_field_rekey_values.sql`). **Renombrar campo huérfana datos**: los ítems guardan `values` por `field.name`; renombrar dejaba la clave vieja. Enfoque elegido **B (re-indexar al renombrar, el más simple)**: RPC `rename_inventory_field` que en una transacción valida `inventory.edit`+propiedad, evita colisión y **re-indexa la clave en los `values` de todos los ítems del área**. `FormBuilder` (onBlur del nombre) llama la RPC. Verificado REST (re-index ✓, colisión 409, miembro sin edit 403) + localhost (rename por UI conserva el valor del ítem). No había huérfanos previos (0 a migrar). | `FormBuilder.jsx:89-101,187-191`; `dynamicInventory.js:232` | l 🔑 |
| P1.10 | ✅ **HECHO Y PROBADO (Sesión 5)** (`20260618_p1_10_premium_auto_expiry.sql`). **Premium vencido seguía activo**. Cliente: `SubscriptionContext.normalizeExpiredPremium()` trata premium+active+vencido como `free` al leer la suscripción → toda la app honra el vencimiento al instante; `isPremium()` también revisa `current_period_end`. BD: `pg_cron` instalado + función `expire_premium_subscriptions_auto` (gemela sin gate de admin, solo cron) agendada a diario (`expire-premium-daily`, 03:00 UTC). **Hallazgo**: ningún consumidor usaba `isPremium()`; la habilitación keyeaba por `plan_id` sin mirar el periodo → la cron + la normalización son el fix real. Verificado: función expira premium-vencido (no toca el vigente) + localhost (periodo vencido oculta pestañas premium). | `AdminPlans.jsx:112-119`; `SubscriptionContext.jsx:391`; `20260611_admin_plan_panel.sql:237` | m 🔑 |
| P1.15 | ✅ **HECHO Y PROBADO (Sesión 7)** (`20260618_p1_15_check_email_availability_ignore_empty_shell.sql`). **Causa raíz verificada en BD**: un miembro NO acumula datos propios (opera bajo el `user_id` del dueño vía `getEffectiveUserId`, [team.js:14](../src/services/team.js)); el cascarón venía de cuando el usuario exploró como **dueño antes** de ser invitado. El bloqueo de reinvitación lo causaba que `check_email_availability` contaba **`inventory_areas`** (andamiaje vacío) como "ser dueño". **Fix BD**: `is_owner` ahora = tiene CONTENIDO real (productos OR transacciones OR movimientos); el andamiaje vacío (áreas/monedas) ya **no** bloquea. **Fix cliente**: `inviteMember` usa **upsert** sobre la `UNIQUE(owner_id, member_email)` → reinvitar tras un `revoked` (downgrade) reactiva la fila en vez de chocar con la restricción única (antes INSERT ciego). **Probado EN VIVO (reversible)** sobre roberyani94: cascarón vacío de 1 área + `revoked` → `check_email_availability`=**true** (reinvitable); +1 producto → **false** (contenido real sí bloquea); owner real con productos → **false**. BD restaurada EXACTA (active, 0 datos). Unit test `team.test.js` actualizado a upsert (4/4). | `Login.jsx`; `check_email_availability`; `team.js inviteMember` | m 🔑 |
| P1.16 | ✅ **HECHO Y PROBADO (Sesión 6b)** (`20260618_p1_16_enforce_plan_locked_area_content.sql`). **Fuga del downgrade premium→free**: el downgrade no borra las áreas excedentes, las marca `plan_locked=true`, pero el guard previo solo cubría la **fila del área**; las RLS de `inventory_items`/`inventory_area_fields`/`inventory_area_prefixes` solo miran permisos → un **free podía crear/editar ítems y campos dentro de sus áreas premium bloqueadas por API** (uso desmedido de premium). **Reproducido EN VIVO** (downgrade real reversible del owner: REST `UPDATE 200`/`INSERT 201` sobre ítems de un área bloqueada). **Fix**: triggers `BEFORE INSERT/UPDATE` (`enforce_locked_area_write`) en las 3 tablas hijas que bloquean escrituras a áreas `plan_locked` (respetan `app.bypass_plan_lock`; lectura y DELETE permitidos); SQLSTATE 53400 → mensaje amigable. **Verificado post-fix**: REST `INSERT` → `500/53400` "Área bloqueada por tu plan". Resto del downgrade ✅ (bloquea excedentes, deja 1 moneda, revoca equipo, sub `free`; el upgrade **auto-desbloquea** vía trigger de `subscriptions`→`apply_subscription_policies`). Límites de creación (áreas/productos/tx/monedas/socios) ya estaban blindados server-side. BD del owner restaurada EXACTA; advisor **0 ERROR**. | `apply_downgrade_to_free`; RLS/triggers `inventory_items` | m 🔑 |
| P1.11 | ✅ **HECHO Y PROBADO (Sesión 6)** (`20260618_p1_11_plan_limits_currencies_partners_serverside.sql`). **Límites de plan solo en cliente** (saltables por API directa). Verificado en BD: `business_currencies` solo gateaba `config.edit` (sin plan → free podía meter N monedas por REST); `team_members` ya exigía `is_premium(owner)` (free=0 cubierto) pero **el tope premium=5 no se aplicaba**. Fix server-side (defensa en profundidad): `get_effective_plan_id(uuid)` (plan efectivo honrando expiración, que `is_premium()` NO mira) + trigger `BEFORE INSERT OR UPDATE` en `business_currencies` (tope monedas activas/plan) + trigger `BEFORE INSERT` en `team_members` (tope socios/plan); `currencies` añadido a `plans.limits` (free=1, premium=-1). Errores con SQLSTATE `53400` (`configuration_limit_exceeded`) → mapeado a mensaje amigable en `notifications.jsx`. **Probado**: REST con cuenta free real (1ª moneda 201, 2ª distinta **bloqueada 53400**); socios vía `DO`/`ROLLBACK` (free no añade ni 1; premium se corta en el 6º). Verificado: ninguna función server-side inserta en estas tablas (no rompe flujos); aceptar invitación es UPDATE (no dispara). BD prístina; advisor **0 ERROR**. | `CurrencyContext.jsx:72`; `TeamManagement.jsx:67-70`; RLS `business_currencies_write`/`team_members_manage` | m 🔑 |
| P1.12 | ✅ **HECHO Y PROBADO (Sesión 6)**. **Recálculo de balance** trataba todo tipo no-`income` como gasto (`else totalExpense += amount`). Fix: `else if (t.type === 'expense')` en `updateBalanceConfig` (coherente con `computeTotals`/`getDashboardStats`, que filtran por tipo explícito). Verificado con **unit test específico** (`finanzas.unit.test.js`, 3/3: un tipo `transfer` de 999 NO resta → `current_balance` queda 150, no -849) + análisis BD (`other`=null: hoy no hay tipos distintos de income/expense → cero impacto en datos reales) + render OK del panel de balance en localhost. (Observación lateral: `current_balance` puede quedar desactualizado porque solo se recalcula al guardar; no es P1.12.) | `finanzas.js` (`updateBalanceConfig`) | s |
| P1.13 | ✅ **HECHO Y PROBADO (Sesión 6)**. **Validación de monto laxa**: `!isNaN(Number(val))` aceptaba `Infinity`, hex (`0x10`), notación científica (`1e3`) y espacios. Fix: zod con regex `^\d{1,12}(\.\d{1,2})?$` (solo dígitos + hasta 2 decimales) + `Number.isFinite(n) && n > 0`; `calculateTotal` guardado para no mostrar `NaN`. **Probado en localhost (móvil 375px + desktop)**: `1e3`/`Infinity`/`0x10`/`-5` rechazados, `100.50` aceptado, sin `NaN` en Total. **Bonus pixel-perfect (§9.8)**: detecté desfase de altura Monto (input `h-11`) vs selects/fecha (`h-9`) en la misma fila grid → igualé los 4 selects + botón de fecha a `h-11` (44px, mejor touch target); filas Tipo/Fecha, Monto/Moneda y Categoría/Método ahora alineadas, sin overflow a 375px. | `TransactionModal.jsx:63-65` | s |

---

## P2 — Seguridad de exportación e integridad de auditoría

| # | Hallazgo | Evidencia | ⏱️ |
|---|---|---|---|
| P2.1 | ✅ **HECHO Y PROBADO (Sesión 3)**. **Inyección de fórmulas (CSV/Excel injection)**: `exportToExcel` pasaba los datos sin sanitizar. Fix: `sanitizeCellValue` antepone `'` a los textos que empiezan con `= + - @` (tab/CR), sin tocar números/importes negativos en string. Verificado con test unitario (`exportUtils.test.js`, 3/3: escapa fórmulas, deja números, la celda queda `t='s'` sin `.f`) + smoke en localhost (exportación genera el `.xlsx`, sin errores de consola). El PDF no aplica (no ejecuta fórmulas). | `src/utils/exportUtils.js` | s |
| P2.2 | ✅ **HECHO Y PROBADO (Sesión 7)**. `audit_logs.details` guardaba la **fila completa** de la transacción (monto, cuenta, referencia, notas, adjuntos) — legible por un rol con `logs.view` pero sin `finanzas.view`, y exportada en claro. **Fix**: `finanzas.js` (crear/actualizar) guarda solo metadatos `{transaction_id, type, category, currency}`. **+Depuración** de los **9** logs históricos con PII al mismo subconjunto (aprobada por Roberto; minimización GDPR). **Probado en localhost (móvil 375px)**: la vista de Logs muestra los detalles limpios, **0 fuga** de monto/cuenta/referencia en los 13 detalles, sin overflow; export usa el mismo `JSON.stringify`. | `finanzas.js:138-146,193-200` | m 🔑 |
| P2.3 | ✅ **HECHO Y PROBADO (Sesión 7)** (`20260618_p2_3_audit_logs_index.sql`). `audit_logs` solo tenía la PK en `id`. Creado `idx_audit_logs_business_created (business_id, created_at DESC)` que sirve exactamente la consulta de Logs (filtra `business_id`, ordena `created_at DESC`). **EXPLAIN confirma `Index Scan using idx_audit_logs_business_created`**; advisor 0 ERROR. | `auditLogs.js:52-53` | **xs** 🔑 |
| P2.4 | ✅ **HECHO Y PROBADO (Sesión 8)** (`e2b0b87`). `xlsx@0.18.5` (CVE-2023-30533 prototype pollution + CVE-2024-22363 ReDoS, ambos en el **parser** `XLSX.read` que GESTIA no usa: solo exporta) → dependencia apuntada al **tarball oficial de SheetJS 0.20.3** (`https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz`), fix recomendado por SheetJS (la versión sin CVE no está en npm). API idéntica → `exportUtils.js` y su test sin cambios. Verificado: `npm ls xlsx@0.20.3`, test de escape 3/3, build OK, runtime en localhost generó `.xlsx` de 16KB sin error. | `package.json:47` | m |
| P2.5 | ✅ **HECHO Y PROBADO (Sesión 7)**. `fmt` (narrativeGenerator) y `CurrencyContext.formatCurrency` ahora hacen `try/catch` → fallback decimal + código si el código de moneda es no-ISO (antes `RangeError` rompía el reporte Word y la previsualización). **Probado**: unit test `narrativeGenerator.test.js` (2/2: código inválido `BITCOIN` no lanza y usa fallback; ISO normal OK) + previsualización del informe en localhost (renderiza "USD 150.00/25.00", **sin NaN/Invalid**). | `narrativeGenerator.js:7-22`; `CurrencyContext.jsx:161-178` | s |
| P2.6 | ✅ **HECHO Y PROBADO (Sesión 7)** (`20260618_p2_6_close_company_logos_listing.sql`). El bucket público `company-logos` tenía `company_logos_public_read` (SELECT para `public` sobre todo el bucket) → listado anónimo abierto. La app muestra el logo por `getPublicUrl`+`<img>` ([businessSettings.js:43-47](../src/services/businessSettings.js)), que **no** pasa por RLS → cerrar el SELECT anónimo no rompe la visualización (patrón P0.6). **Fix**: política SELECT `authenticated` + carpeta propia. **Verificado**: anon (rol `anon` por RLS) lista → **0**; URL pública del logo → **200 image/png** (se sigue viendo); advisor `public_bucket_allows_listing` desaparece (0 ERROR). | `get_advisors(security)`; `businessSettings.js` | m 🔑 |

---

## P3 — Experiencia premium, color y modo oscuro (tu objetivo de imagen)

| # | Hallazgo | Evidencia | ⏱️ |
|---|---|---|---|
| P3.1 | ✅ **HECHO Y PROBADO (Sesión 8)** (`6419ef5`). Nada cargaba (index.html solo pedía JetBrains Mono; Tailwind decía Geist; index.css Outfit) → unificado a **UNA fuente: Outfit** (geométrica/premium, Google Fonts): cargada en `index.html`, Tailwind sans+heading→Outfit, h1-h6→`var(--font-sans)`. Verificado: `document.fonts.check('16px Outfit')=true`, body/headings en Outfit. | `tailwind.config.js:12-13`; `index.css:41,98,149`; `index.html:12-20` | s |
| P3.2 | ✅ **HECHO Y PROBADO (Sesión 8)** (`a9e6484`). Finanzas/Reportes/MovementList usaban `text-green-600`/`text-red-600` (+ `bg-green-100`/`bg-red-100`) hardcodeados → tono distinto al Dashboard y rotos en oscuro. Estandarizado a `text-success`/`text-destructive` + `bg-success/10`, `bg-destructive/15`. Los iconos verde/rojo de los botones de export (Excel/PDF) se dejan (tipo-de-archivo, no semántica financiera). Verificado en oscuro y claro, sin overflow. | `FinanzasMejorado.jsx`; `MovementList.jsx`; `Reportes.jsx` | m |
| P3.3 | ✅ **HECHO Y PROBADO (Sesión 8)** (`b85bcfe`). `ForgotPassword`/`ResetPassword` hardcodeaban `bg-white`/`bg-gray-*`/`text-gray-*` + banners verde/rojo/azul → caja blanca/texto ilegible en oscuro. Migrado a tokens (`bg-background`/`bg-card`/`border-border`/`text-foreground`/`text-muted-foreground`, inputs `bg-background border-input`, banners `bg-destructive\|success/10`, fortaleza `bg-muted`+`bg-success/warning/destructive`). Verificado en oscuro: `.bg-white`=0, sin overflow, en ambas páginas + formulario. | `ResetPassword.jsx`; `ForgotPassword.jsx` | m |
| P3.4 | ✅ **HECHO Y PROBADO (Sesión 8)** (`093ba12`). `ErrorBoundary` global (`src/components/common/ErrorBoundary.jsx`) envuelve `<App/>` en `main.jsx` (dentro de los providers). Captura errores de render (antes p.ej. un error en `ProtectedRoute` dejaba pantalla en blanco) y muestra un fallback premium mobile-first con tokens (modo claro/oscuro), botones Recargar/Volver al inicio y "Detalles técnicos" colapsable. Test unitario 2/2 (fuerza error→fallback; sin error→hijos). Verificado en localhost forzando `?boom=1` (desktop+móvil 375px, sin overflow). | `src/main.jsx`; `ErrorBoundary.jsx` | s |
| P3.5 | ✅ **HECHO Y PROBADO (Sesión 9)** (`bff1b90`). Bundle único de 2,612 KB (772 KB gzip) → **code-splitting**: páginas con `React.lazy`+`Suspense` (el Suspense va alrededor del `<Outlet/>` del `SidebarLayout` para que el sidebar persista); `manualChunks` separa vendors (recharts→`charts`, jspdf/xlsx/docx/html2canvas→`export-vendor`, supabase, react-router); `exportUtils` con `import()` dinámico en `finanzas.js`/`LogsMejorado` (solo al exportar). **Carga inicial ~150 KB index + 170 KB supabase (~90 KB gzip)**; `export-vendor` (455 KB gzip) y `charts` (160 KB gzip) bajo demanda. Verificado: build con chunks separados + navegación en localhost sin errores; suite 27/27. | `npm run build` | m |
| P3.6 | ✅ **HECHO Y PROBADO (Sesión 9)** (`ea135c3`). **Contraste WCAG**: el verde `--success` en modo claro sobre fondo blanco daba **2.54:1 (FALLA AA)** → oscurecido a `160 90% 26%` = **4.98:1** (verificado en vivo 4.96:1); modo oscuro ya cumplía (10.75:1), sin cambios. **Daltonismo**: signo **+/−** en los importes de la lista de transacciones y tarjetas de resumen de Finanzas (antes solo color+icono); Reportes/MovementList ya tenían +/−; tarjetas del Dashboard ya tienen icono+título. | research | m |
| P3.7 | ✅ **HECHO Y PROBADO (Sesión 9)** (`d592d96`). Hook `useChartAnimation`: `animationDuration` 400ms (antes default 1500ms) + `ease-out`, respeta `prefers-reduced-motion` (desactiva si el usuario lo pide), desactiva animación en datasets grandes (>60 pts). Aplicado a las series de `Dashboard` (2 Bar + Pie) y `AlmacenDashboard` (Pie + 2 Line + Bar). Verificado: 6 superficies, 28 elementos de serie, sin errores. | research | s |
| P3.8 | ✅ **HECHO Y PROBADO EN LOCALHOST (Sesión 10, móvil 375px + desktop)** — Roberto aprobó "rediseño completo". `Dashboard.jsx` reescrito (capa de datos intacta): **1 tarjeta héroe** "Tu dinero disponible" (moneda principal grande + resto en pequeño) + **3 de apoyo** ("Entró este mes" +verde, "Salió este mes" −rojo, "Resultado del mes" con icono según signo + "Vas ganando/Vas en números rojos/En equilibrio"). **Selector de moneda único del panel** (gobierna héroe, tarjetas y ambas gráficas; elimina 2 selectores de moneda redundantes y el apilado de N monedas por tarjeta). Lenguaje llano: h1 "¿Cómo va tu negocio?". **Empty state guiado en 3 pasos** (configura moneda → registra movimiento → vuelve; TTV<5min) cuando no hay actividad. La 5ª tarjeta ("Movimientos") → barra "N operaciones registradas este mes". Bugs corregidos: icono fijo `TrendingDown` (ahora dinámico por signo) y Pie `outerRadius={30}`→ donut `innerRadius=55/outerRadius=95`. Verificado: 3 tarjetas de **igual altura exacta** (122px desktop) y **alineadas/sin overflow** a 375px; consola sin errores; ambos estados (con datos y vacío) por `?empty=1` temporal (revertido). | `src/pages/Dashboard.jsx` | l |
| P3.10 | ✅ **HECHO Y PROBADO (Sesión 6, UX Configuración reportada por Roberto; client-only, sin BD)**. Lote de mejoras al módulo Configuración, probado a **375px y desktop** (sin overflow, alineado): **(C1)** botones "Guardar Moneda Principal"/"Actualizar Balance Inicial" movidos al **final** de la sección de balance (`BalanceBaseSection.jsx`, antes a la altura del título). **(C2)** tab Monedas: etiqueta **"Activar"** junto al switch + **tooltip** en "Hacer principal" (aclara que es la moneda que se conserva al bajar de plan) — `CurrenciesPanel.jsx`. **(C3)** equipo: estados reales **Activo/Pendiente/Revocado** con badges distintos (antes 'revoked' se mostraba como "Pendiente") — `TeamManagement.jsx`. **(C4)** verificado que cambio de rol y baja se reflejan al instante en la vista del dueño (estado optimista; tiempo real para el miembro queda como fase aparte). **(C5)** tab Roles: las tarjetas muestran los permisos **en español** (descripción de la tabla `permissions`, ya en español) en vez de códigos como `logs.view` — `RoleManagement.jsx`. **(C6)** eliminado el tab **"Permisos"** (no gestionaba nada) — `ConfiguracionMejorado.jsx`. **(C7)** roles del **sistema** ahora tienen botón **"Ver permisos"** que abre el `RoleModal` en **solo-lectura** (prop `readOnly`: campos/checkboxes deshabilitados, footer "Cerrar"); los personalizados mantienen Editar/Eliminar. **(C8)** la **tabla de Equipo** se convierte en **tarjetas en móvil** (`md:hidden` con email/estado/rol/Eliminar accesibles; tabla `hidden md:block` en desktop) → sin scroll horizontal. **(C9)** quitado el botón **"Hacer principal"** del tab Monedas (la principal se define sólo en General; queda sólo "Activar"+switch). **(C10)** el tab **Planes** muestra el **plan activo primero** (`PlansPanel` ordena por `currentPlan`). | `BalanceBaseSection.jsx`; `CurrenciesPanel.jsx`; `TeamManagement.jsx`; `RoleManagement.jsx`; `RoleModal.jsx`; `ConfiguracionMejorado.jsx`; `PlansPanel.jsx` | m |
| P3.9 | ✅ **HECHO Y PROBADO (Sesión 5, UX inventario reportada por Roberto)**. (a) **Bug del asterisco**: en `FormRunner.jsx` solo los campos de **texto** mostraban el indicador requerido; número/fecha/color/lista/sí-no no mostraban nada → un campo requerido (p.ej. "Cantidad") se veía igual que uno opcional. Fix: helper `FieldRequiredMark` (reutiliza el patrón existente de texto) aplicado a TODOS los tipos. (b) **Tipos menos técnicos** en `FormBuilder.jsx`: "Booleano"→"Sí / No", "Área de Texto"→"Texto largo", "Selección"→"Lista de opciones" + **pista corta** bajo el selector de Tipo (dinámica por tipo). (c) **"Requerido" → casilla "¿Obligatorio?"** (más intuitiva) en "Agregar Campo" y en la fila de edición (muestra Sí/No). (d) **Empty state guiado** cuando un área no tiene campos. (e) **Rediseño mobile-first/pixel-perfect** de "Agregar Campo" (apila en móvil, 2 columnas en desktop, sin desbordes ni desalineación por la pista). Solo etiquetas/UI; el `type` guardado no cambia. Probado en localhost a **móvil (375px) y desktop (1024/1100px)**: sin overflow horizontal, todo alineado; la casilla crea el campo con `required=true` (verificado en BD). **Nueva regla en CLAUDE.md §9.8: UI mobile-first y pixel-perfect.** | `FormRunner.jsx`; `FormBuilder.jsx` | s |

---

## P4 — Higiene de migraciones y código muerto (antes de tocar la BD)

| # | Hallazgo | Evidencia | ⏱️ |
|---|---|---|---|
| P4.1 | 🔶 **PROPUESTO (Sesión 10) — pendiente de tu decisión.** Verificado: la carpeta local `supabase/migrations/` tiene **~63 archivos** (toda la genealogía, incl. `fix_rls_recursion`, `fix_team_members_rls_*`), pero el remoto solo registra **34 migraciones** aplicadas → **el repo NO está sincronizado con `schema_migrations`**. **Matiz clave**: no hay `supabase` CLI instalado (memoria infra) y los despliegues van por **MCP `apply_migration`** (asigna su propio `version`), así que el orden de los FICHEROS hoy **no** dirige despliegues → no es un riesgo activo, es higiene/reproducibilidad. Propuesta: (A) renombrar los ficheros sin fecha con su `version` real del remoto (`rbac_hardening_v2.sql`→`20260604144624_…`, etc.) y (B) archivar la genealogía no aplicada en `supabase/_migrations_archive/` con README. NO tocado aún. | `supabase/migrations/` | m |
| P4.2 | ✅ **VERIFICADO EN PROD (Sesión 10, read-only)** + 🔶 consolidación documental pendiente. **`pg_policies` reales confirmados SANOS**: ninguna de las 24 tablas tiene rol `anon` en políticas; conteos correctos (4 CRUD / 2 ALL+SELECT); **0 políticas de escritura permisivas incondicionales** (`qual/with_check='true'`); advisor seguridad **0 ERROR**. El estado canónico de `rbac_hardening_v2.sql` es el vivo; no quedan restos de la era `fix_*_rls`. La "consolidación" restante es **documental** (archivar los `fix_*` superados del repo, ver P4.1), no hay deuda de RLS en la BD. | `pg_policies` (MCP) | l 🔑 |
| P4.3 | ✅ **HECHO (Sesión 10)**. `schema.sql` raíz (prototipo obsoleto: `products` sin `user_id`, currency solo USD/CUP, trigger `trg_movements_update_stock` que **duplicaría** el stock vs la RPC atómica `register_stock_movement`) **movido a `supabase/_PELIGROSO_NO_USAR/schema.sql`** con fila nueva en el README de cuarentena. No estaba en `migrations/` (no se aplicaba por CLI) ni se referencia en código. | `supabase/_PELIGROSO_NO_USAR/schema.sql` | s |
| P4.4 | ✅ **HECHO Y PROBADO EN LOCALHOST (Sesión 10)**. Opciones de auth explícitas en `src/config/supabase.jsx` (`persistSession`/`autoRefreshToken`/`detectSessionInUrl`, con comentario). Igualan los defaults de supabase-js v2 (documentan intención + blindan ante cambios de la lib). Verificado: la sesión del owner **persiste** entre recargas en localhost. | `src/config/supabase.jsx:7` | xs |
| P4.5 | ✅ **HECHO Y PROBADO EN LOCALHOST (Sesión 10) — client-only (NO requiere BD)**. Verificado: `updatePlan` **no lo llama ningún componente** (solo está en el contexto + mockeado en tests); el botón "Cambiar a Gratuito" de `PlansPanel` ya enruta por el asistente (`DowngradeToFreeDialog`→RPC `apply_downgrade_to_free`). Se añadió un **guard** en `updatePlan('free')` (paralelo al de `'premium'`) que cierra el footgun del UPDATE directo y dirige al asistente. Probado en localhost: el botón abre el wizard (Paso 1 de 4), no un downgrade directo. La marca 🔑 del plan era precautoria. | `SubscriptionContext.jsx:255` | s |
| P4.6 | ✅ **HECHO Y PROBADO (Sesión 10)**. Eliminado código muerto verificado por grep (sin consumidores): `AppMejorado.jsx`, `pages/ReportesMejorado.jsx` (solo re-exportaba `Reportes`), `services/reports.js`, `App.css` (tema teal legacy, no importado), `pages/index.js` (barrel ya no usado tras code-splitting). **El "2º PermissionGuard" NO se borró: ambos están en uso** — `SubscriptionGuard.jsx` (default `PermissionGuard`) lo usa `SidebarLayout`; `common/PermissionGuard.jsx` (named) lo usan las páginas. Build+27/27+smoke OK. | varios | s |
| P4.7 | ✅ **HECHO Y PROBADO (Sesión 10)**. Quitadas deps con **0 imports** en `src`: `firebase`, `geist`, `moment`, `react-datepicker`, `@headlessui/react`, `react-scripts`; self-dep circular `sistema-citas`; `name`→`gestia`; +`firebase-debug.log`. **Hallazgo**: `react-scripts` era el **único proveedor** de `eslint@8.57` + `eslint-config-react-app@7` → añadidos como **devDeps explícitas** para no romper `npm run lint` (mismas reglas). `npm install`: **−858 paquetes** (816 quedan). Lint+build+27/27 OK. | `package.json` | s |

---

## P5 — Tests y verificación

| # | Tarea | ⏱️ |
|---|---|---|
| P5.1 | ✅ **HECHO Y PROBADO (Sesión 10)**. Raíz del flake: el proyecto **no tenía auto-cleanup global** de Testing Library (`globals` off + sin `afterEach(cleanup)` global) → DOM filtrado entre tests volvía ambiguos selectores como `'Solicitar Premium'`. Fix: `test.globals: true` en `vite.config.js` + `afterEach(() => cleanup())` en `setupTests.js` (cleanup centralizado para todos los archivos). **Verificado: 3 corridas completas seguidas 27/27 estables** (PlansPanel ~2s, sin fallo). | s |
| P5.2 | ✅ **HECHO Y PROBADO (Sesión 10)**. +`TransactionModal.test.jsx` (3): **P1.2 readonly** (título "Ver Movimiento", solo botón "Cerrar", campos deshabilitados vía `<fieldset disabled>`; modo editable muestra "Revisar Datos") y **P1.1 moneda** (usa la moneda por defecto del negocio, no el USD fijo). +`notifications.test.js` (6): `getSupabaseErrorMessage` mapea **23514** (stock negativo, P1.6/P1.7), **53400** (límite de plan con/sin mensaje, P1.11), 23505/23503/42501, `null` y `Failed to fetch`. Escape de Excel (P2.1) ya tenía test. **Suite 27→36 (12 archivos).** | m |
| P5.3 | **Probar cada cambio en `localhost`** (`npm run dev`) antes de aprobarlo (regla de Roberto). Para flujos con BD, verificar también con datos reales. | — |

---

## 🚀 Ruta recomendada (la más eficiente)
1. **Quick wins visibles sin BD** (1 sesión, mucho impacto): P1.1 (moneda), P3.1 (fuente), P3.2 (color tokens), P3.4 (ErrorBoundary), P2.1 (escape Excel). Probar en localhost.
2. **Verificación de seguridad en prod** (en cuanto tenga `.env.tooling`): P0 completo — confirmar RLS y limpiar scripts peligrosos.
3. **RBAC y datos** (P1.2–P1.10) + **higiene de migraciones** (P4.1) antes de cualquier migración nueva.
4. **Integridad/auditoría** (P2.2–P2.5) y **premium/UX** (P3.3, P3.5–P3.8).
5. **Limpieza** (P4.6–P4.7) y **tests** (P5).

> Nada se marca como "terminado" sin probarse en `localhost`. Las tareas 🔑 requieren que rellenes `.env.tooling` (ver `CONEXIONES.md`).
